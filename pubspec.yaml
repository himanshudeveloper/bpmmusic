name: bmp_music
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:

  cloud_firestore: ^5.0.0
  # cloud_functions: ^5.0.0
  # crypto: ^3.0.3
  dio: ^5.4.3+1
  firebase_auth: ^5.0.0
  firebase_core: ^3.0.0
  flutter:
    sdk: flutter
  # flutter_dotenv: ^5.1.0
  get: ^4.6.6
  google_fonts: ^6.2.1
  intl: ^0.19.0
  # just_audio: ^0.9.38
  permission_handler: ^11.3.1
  # provider: ^6.1.2
  shared_preferences: ^2.2.3
  sign_in_with_apple: ^6.1.0
  transparent_image: ^2.0.1
  # webview_flutter: ^4.8.0
  # music_kit: ^1.1.0
  pretty_dio_logger: ^1.3.1
  hooks_riverpod: ^2.4.10
  flutter_hooks: ^0.20.5
  persistent_bottom_nav_bar: ^6.2.1

  riverpod_annotation:
  fluttertoast: ^8.2.6
  audio_video_progress_bar: ^2.0.3
  audio_service: ^0.18.15
  just_audio: ^0.9.39

  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.9
  riverpod_generator:
  freezed: ^2.4.7
  

dependency_overrides:
  music_kit:
    path: ./package/music_kit
  music_kit_platform_interface:
    path: ./package/music_kit_platform_interface

flutter:
  uses-material-design: true

  assets:
    - assets/
